import styled from 'styled-components';

const Header = styled.h1`
  font-weight: bold, 70;
  color:       #1e1e1e;
  font-family: Helmet,Freesans,Helvetica,Arial;
  font-size:   2.25rem;
  font-size:   36px;
`;

export default Header;
